scopes:
  source:
    manual:

queue_rules:
  - name: default
    queue_conditions:
      - check-success = integration
    merge_conditions:
      - check-success = integration

pull_request_rules:
  - name: auto queue external fork PRs
    conditions:
      - from-fork
    actions:
      queue:
        name: default
